line-length = 100
target-version = "py310"
extend-exclude = ["build", "dist", ".venv"]

[lint]
select = [
    "E", "F", "W",   # pycodestyle + pyflakes
    "I",             # isort
    "B",             # bugbear
    "UP",            # pyupgrade
    "SIM",           # simplify
    "RUF",           # ruff-specific
    "D",             # pydocstyle
    "ANN",           # annotations
]
ignore = [
    "D203",   # 1 blank line before class docstring (conflicts with D211)
    "D213",   # multi-line summary second line (conflicts with D212)
    "ANN401", # Any at module boundaries (ebooklib has no typing)
]

[lint.per-file-ignores]
"tests/**" = ["D", "ANN"]

[lint.pydocstyle]
convention = "google"

[format]
quote-style = "double"
indent-style = "space"
